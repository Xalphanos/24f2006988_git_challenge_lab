from src.sum import add
from src.difference import sub
from src.product import mul

if __name__ == "__main__":
    x, y = 7, 5
    print("sum:", add(x, y))
    print("difference:", sub(x, y))
    print("product:", mul(x, y))
